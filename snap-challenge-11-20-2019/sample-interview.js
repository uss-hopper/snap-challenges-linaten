function counts ($array) {
	$newArray = [];
	foreach( $array as $element)
	$newArray= [$element = $newArray[$element] +1];
}else{

echo($element, $newArray[$element])
};
return $newArray;

$origArray = [1,1,2,3,3,4,4,4,4]
var_dump(counts($origArray));




const int = [1,3,5,6,7,4,8,9];
int count_occur(int a [],int num_elements, int value)
{
	int i, count = 0;
	for (i = 0, i<num_elements; i++)
	{
		++count;
		a[i] = INFINITY;
	}
}
return(count);
}

for( var i=1; i<21; i++ ) {
	if( (i%3)===0 && (i%))
}

